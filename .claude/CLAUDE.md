# Workspace-Level AI Agent Rules

All workspace rules, guidelines, and conventions are defined in `AGENTS.md` at the repository root.

See `AGENTS.md` for complete project guidelines.

## 铁律

- `governance/` 下的任何文件都必须精简，禁止废话，保障文档可读性并减少 token 消耗。
