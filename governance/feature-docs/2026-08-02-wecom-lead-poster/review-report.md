# Review Report

## 结论

实现覆盖了 MVP 约定：企业微信联系我配置同步、背景图上传、前端二维码编辑/导出、商圈筛选元数据、OSS 最终图保存；没有加入文案维护、AI 生成和小程序对接。

## 已检查项

- 后端通过 mp、store-biz、infra-biz 编译。
- 前端生产构建确认 Canvas 编辑器可打包，后端编译确认不再包含海报图片处理逻辑。
- 联系我同步使用 `list_contact_way` + `get_contact_way`，二维码先保存文件快照。
- 海报保存由前端通过现有文件上传接口上传最终 PNG，后端只保存 URL 和二维码坐标/尺寸。
- 管理后台生产构建和本次新增文件 ESLint 通过。
- 菜单名为“引流海报”，权限和升级 SQL 已增加。

## 验证缺口与残余风险

- `pnpm ts:check` 受仓库基线类型声明配置影响未通过，需后续单独修复基线配置后重跑。
- 真实企微 API、OSS 上传、二维码识别和多租户数据需要测试环境联调。
- 当前背景图使用 Canvas cover 方式填充 3:4 画布；如果实际素材主体经常不在中心，需要后续增加背景裁切参数。
- 图片删除和历史海报清理未纳入 MVP，OSS 会保留旧文件。
