# 会员 ID 查询

## 后端接口

会员分页接口 `GET /admin-api/member/user/page` 新增可选查询参数 `id`：

- `id`：会员内部 ID，Long，精确匹配。
- 未传值：保持现有分页查询行为。
- `id` 可与昵称、手机号、标签、登录类型和企微客户筛选组合使用，多个条件按 AND 关系过滤。

## 前端

管理端会员管理搜索栏新增“会员 ID”输入项，提交分页查询时传递 `id` 参数；清空后不传该参数。
