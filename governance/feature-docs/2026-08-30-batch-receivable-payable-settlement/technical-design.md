# 技术设计：应收应付批量结算

1. 管理端新增批量结算请求与汇总响应对象，校验 ID 列表非空且不超过 100 条。
2. 应收应付服务新增批量编排方法，按去重后的 ID 顺序调用既有 `settle(id)`；每项捕获可恢复的业务/外部异常并形成失败结果。
3. 批量接口继续使用 `pay:receivable-payable:settle` 权限，单条接口同步使用该权限。
4. 管理端表格使用 Element Plus selection，并通过现有 `canSettle` 规则禁用不可结算记录。

批量接口不改变现有结算幂等、退款校验、租户隔离和 AdaPay 请求边界。
