# 变更报告：应收应付批量结算

- `backend`：新增批量结算管理端接口、请求/响应对象和逐项执行编排；支持去重、单项失败隔离和 100 条上限。
- `admin`：应收应付列表增加可结算记录多选和“批量结算”按钮，复用结算权限与状态判断。
- `governance`：新增批量结算需求、接口契约、技术设计、UI 设计和测试计划。

不新增表结构，不改变既有单条结算接口和 AdaPay 外部请求语义。
