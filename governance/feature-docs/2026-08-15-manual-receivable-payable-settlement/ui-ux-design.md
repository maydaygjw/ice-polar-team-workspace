# UI/UX：手工触发应收应付结算

应收应付列表新增“结算”操作列：

- `SUCCESS`、`SUCCESS_WITH_DIFFERENCE` 且为 Adapay 订单：按钮可用。
- 已结算、处理中、失败或非 Adapay 订单：按钮禁用，并显示对应原因或状态。
- 点击后弹出确认提示；请求成功刷新当前列表，失败显示后端返回的失败提示。
- 保留“重算失败订单”能力；结算失败不改变应收应付计算结果。
