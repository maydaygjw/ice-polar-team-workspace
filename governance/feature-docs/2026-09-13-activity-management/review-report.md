# 活动管理实现审查

一期已覆盖模板、周期期次、奖品固化、报名、随机开奖、中奖记录、定时开奖、二维码和 admin 管理页面。数据表具备租户隔离、软删除、唯一约束及关键索引。

上线前需执行真实 MySQL 迁移、确认营销父菜单 ID，并用真实租户/会员/企微本地快照验证报名校验。小程序 UI 不在一期范围内；二维码接口已预留 `pages/activity/detail` 路径。

backend 活动分支已合并最新 `master` 的 Checkstyle 配置，并在 activity 聚合模块启用同一检查 profile；按要求已开启 `failOnViolation=true`，规范违规会阻断构建。当前尚有 226 条规范告警，需继续清理后才能通过门禁。
